import { Box } from "@material-ui/core";
import styles from '@styles/components/gameSettings.module.scss';

const GameSettings = () => {
  return (
    <Box className={styles.settings}>
      settings...
    </Box>
  )
};

export default GameSettings;
